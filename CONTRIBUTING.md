# Contributing to this project

## Developer guide

### Directories

- `chart/templates/` - manifests to be generated
- `chart/templates/tests/` - pod to be created on `helm test` (after chart installed)
